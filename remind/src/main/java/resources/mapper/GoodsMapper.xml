<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- GoodsMapper.xml -->
<mapper namespace="GoodsMapper">
	<select id="selectList" resultType="com.remind.test.vo.GoodsVO">
		SELECT * FROM GOODS_INFO
  		<where>
  			<if test='giNum!=null and giNum!=0'>
  				AND GI_NUM = #{giNum}
  			</if>
  			<if test='giName!=null and giName.length > 0'>
  				AND GI_NAME = #{giName}
  			</if>
  			<if test='giPrice!=null and giPrice > 0'>
  				AND GI_PRICE = #{giPrice}
  			</if>
  			<if test='giDesc!=null and giDesc.length > 0'>
  				AND GI_DESC = #{giDesc}
  			</if>
  		</where>
	</select>
	<select id="selectGoods" resultType="com.remind.test.vo.GoodsVO">
		SELECT * FROM GOODS_INFO WHERE GI_NUM = #{giNum}
	</select>
	<insert id="insertGoods"
		parameterType="com.remind.test.vo.GoodsVO">
		INSERT INTO GOODS_INFO (GI_NAME, GI_PRICE, GI_DESC) VALUES
		(#{giName},#{giPrice},#{giDesc})
	</insert>
	<update id="updateGoods">
		UPDATE GOODS_INFO SET GI_NAME = #{giName}, GI_PRICE=#{giPrice},
		GI_DESC=#{giDesc} WHERE GI_NUM = #{giNum}
	</update>
	<delete id="deleteGoods">
		DELETE FROM GOODS_INFO WHERE GI_NUM = #{giNum}
	</delete>
</mapper>